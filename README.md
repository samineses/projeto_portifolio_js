# Do Projeto
Criamos a estrutura inicial do seu portfólio usando HTML e CSS.
Identificamos e corrigimos o erro de conexão do CSS com o HTML (<link rel="stylesheet">).
Configuramos a execução no VS Code usando F5 com launch.json.

# Remember git commands
- git init
- git config user..
- git remote add origin <repo-url>
- git checkout -b main
- git add
- git commit -m ""
- git commit --amend
- git push origin main


# testes teste
